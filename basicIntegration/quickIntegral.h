#ifndef _quickIntegral_h
#define _quickIntegral_h

char * solveAnti (char * input); //solves the anti-derivative. 
int integrate (char * input, int leftBound, int rightBound); //solves an integral using the AST.

#endif