#ifndef _quickIntegral_h
#define _quickIntegral_h

inline char * solveAnti (char * input); //solves the anti-derivative. 
inline int integrate (char * input, int leftBound, int rightBound); //solves an integral using the AST.

#endif
